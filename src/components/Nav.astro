---
import NavLink from './NavLink.astro';
import type { Menu } from '@/utils/types'

type Props = {
  menu: Menu
}

const { menu } = Astro.props as Props
const { menuItems } = menu ?? {}
---

<nav class="ps-6">
        <button
            class="menu-toggle w-8 h-8 -ml-1 flex items-center justify-center relative z-30 lg:hidden"
            aria-label="Open Menu"
            aria-expanded="false"
            aria-controls="menu-items"
        >
            <span class="menu-toggle-icon w-6 h-0.5 relative bg-current" />
        </button>
        <ul class="menu flex gap-x-8">
            {menuItems?.nodes.map((link) => (
                <li class="py-1">
                    <NavLink
                        class="font-semibold py-1 px-2 rounded-md transition-colors duration-300 ease-smooth bg-transparent hover:bg-green-dark hover:text-green-bis [&.is-active]:bg-green-dark [&.is-active]:text-green-bis"
                        href={link.uri}
                    >
                        {link.label}
                    </NavLink>
                </li>
            ))}
        </ul>
</nav>

<style>
    @reference "../styles/global.css";

    @media (max-width: 1025px) {
        .menu {
            @apply flex-col gap-1 absolute top-22 bg-green-bis right-0 max-w-64 px-3 py-6 z-20 border border-dashed  invisible opacity-0;
            width: calc(100% + 1.25rem);
        }
        .menu.is-visible {
            @apply visible opacity-100;
            transition: opacity 0.15s cubic-bezier(0.4, 0, 0.2, 1) 0.08s;
        }
        .menu-toggle-icon {
            transition: width 0.06s cubic-bezier(0.4, 0, 0.2, 1) 0.12s;
        }
        .menu-toggle.is-active .menu-toggle-icon {
            @apply w-0;
            transition: width 0.06s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .menu-toggle-icon:before,
        .menu-toggle-icon:after {
            @apply w-6 h-0.5 absolute left-1/2 top-0 origin-center -translate-x-1/2 bg-current;
            content: '';
            transition:
                transform 0.12s cubic-bezier(0.4, 0, 0.2, 1),
                margin 0.12s cubic-bezier(0.4, 0, 0.2, 1) 0.08s;
        }
        .menu-toggle-icon:before {
            @apply -mt-1.5;
        }
        .menu-toggle-icon:after {
            @apply mt-1.5;
        }
        .menu-toggle.is-active .menu-toggle-icon:before,
        .menu-toggle.is-active .menu-toggle-icon:after {
            @apply mt-0;
            transition:
                margin 0.12s cubic-bezier(0.4, 0, 0.2, 1) 0.04s,
                transform 0.12s cubic-bezier(0.4, 0, 0.2, 1) 0.12s;
        }
        .menu-toggle.is-active .menu-toggle-icon:before {
            @apply rotate-45;
        }
        .menu-toggle.is-active .menu-toggle-icon:after {
            @apply -rotate-45;
        }
    }
</style>

<script>
    function menuToggle() {
        const menu = document.querySelector('.menu');
        const menuToggleBtn = document.querySelector('.menu-toggle');
        menuToggleBtn?.addEventListener('click', () => {
            const isMenuExpanded = menuToggleBtn.getAttribute('aria-expanded') === 'true';
            menuToggleBtn.classList.toggle('is-active');
            menuToggleBtn.setAttribute('aria-expanded', isMenuExpanded ? 'false' : 'true');
            menuToggleBtn.setAttribute('aria-label', isMenuExpanded ? 'Open Menu' : 'Close Menu');
            menu?.classList.toggle('is-visible');
        });
    }
    menuToggle();
    document.addEventListener('astro:after-swap', menuToggle);
</script>
